---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
---

<BaseLayout title="About">
  <Hero title="About Me" />

  <section class="section">
    <div class="container">
      <div class="about-layout">
        <div class="about-text">
          <h2 class="section-title">Contemporary Artist, Modern Flair</h2>         
          <p>Each of my pieces begins with careful observation of light and shadow, building layers of meaning through the interplay of illumination and darkness. The gothic elements that often appear in my work—from weathered books to antique vessels—serve not as mere subjects but as vessels for deeper contemplation about mortality, memory, and the weight of history.</p>
          
          <p>My artistic practice is rooted in traditional techniques while embracing contemporary sensibilities. I work primarily in oils, building up rich, luminous surfaces that capture the subtle gradations of light that define tenebristic painting. The result is work that feels both timeless and urgently contemporary, speaking to our current moment while honoring the long tradition of still life painting.</p>

          <h3 class="card-title">Artistic Philosophy</h3>
          <blockquote>
            <p>"I'm drawn to the way light can transform the mundane into something profound. In the interplay between shadow and revelation, everyday objects become carriers of meaning, inviting us to slow down and truly see—rather than merely observe—what's before us."</p>
          </blockquote>
        </div>
        
        <div class="about-sidebar">
          <div class="artist-portrait">
            <img src="/imogenclaire.jpeg" alt="Portrait of Imogen Claire in her studio" />
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .about-layout {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--content-gap);
    align-items: start;
  }

  .about-text h2 {
    margin-bottom: var(--space-lg);
  }

  .about-text h3 {
    margin: var(--space-xl) 0 var(--space-md) 0;
    color: var(--deep-burgundy);
  }

  .about-text p {
    font-size: clamp(1rem, 2.2vw, 1.1rem);
    line-height: 1.8;
    color: var(--charcoal-black);
    margin-bottom: var(--space-md);
  }

  .about-text blockquote {
    background: var(--bone);
    padding: var(--space-lg);
    border-radius: 8px;
    margin: var(--space-xl) 0;
    border-left: 4px solid var(--deep-burgundy);
  }

  .about-text blockquote p {
    font-style: italic;
    font-size: clamp(1.1rem, 2.5vw, 1.2rem);
    color: var(--charcoal-black);
    margin-bottom: 0;
    line-height: 1.7;
  }

  .about-sidebar {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    position: sticky;
    top: calc(var(--space-3xl) + 20px);
  }

  .artist-portrait {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    transition: all 0.3s ease;
  }

  .artist-portrait:hover {
    box-shadow: 0 8px 30px rgba(0,0,0,0.15);
    transform: translateY(-2px);
  }

  .artist-portrait img {
    width: 100%;
    height: auto;
    aspect-ratio: 4/5;
    object-fit: cover;
    display: block;
  }

  @media (max-width: 768px) {
    .about-layout {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .about-sidebar {
      order: -1;
      position: static;
      align-items: center;
    }

    .artist-portrait {
      max-width: 350px;
      width: 100%;
    }

    .about-text blockquote {
      padding: var(--space-md);
      margin: var(--space-lg) 0;
    }
  }

  @media (max-width: 480px) {
    .about-layout {
      gap: var(--space-lg);
    }

    .artist-portrait {
      max-width: 300px;
    }

    .about-text blockquote {
      padding: var(--space-sm);
      margin: var(--space-md) 0;
    }

    .about-text h3 {
      margin: var(--space-lg) 0 var(--space-sm) 0;
    }
  }
</style>